@code {
    private bool visible;

    public bool Visible
    {
        get => visible;
        set
        {
            visible = value;
            StateHasChanged();
        }
    }

    public virtual bool Validate() => true;
}
