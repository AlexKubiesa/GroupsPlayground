<UserControl x:Class="GroupsPlayground.UI.GroupPropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GroupsPlayground.UI"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300" d:Background="White" d:DataContext="{d:DesignInstance local:GroupPropertiesViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <local:ValidatedPropertyView Margin="4" PropertyLabel="Group name:" DataContext="{Binding GroupName}"/>
            <local:ValidatedPropertyView Margin="4" PropertyLabel="Group size:" DataContext="{Binding GroupSize}"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Back" Margin="4" Padding="4" IsEnabled="False"/>
            <Button Content="Next" Margin="4" Padding="4"
                    Command="{Binding NextCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
