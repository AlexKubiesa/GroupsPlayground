<UserControl x:Class="GroupsPlayground.UI.GroupLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GroupsPlayground.UI"
             xmlns:domain="clr-namespace:GroupsPlayground.Domain;assembly=GroupsPlayground.Domain"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:Background="White" d:DataContext="{d:DesignInstance local:GroupLibraryViewModel}">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type domain:Group}">
            <StackPanel>
                <TextBlock Text="{Binding Name}" FontSize="20" Margin="4"/>
                <TextBlock Text="{Binding Elements.Count, StringFormat='Size: {0}'}" Margin="4"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <StackPanel MaxWidth="200">
        <ListBox ItemsSource="{Binding Groups}"/>
        <Button Content="Create new group" Command="{Binding CreateGroupCommand}"/>
    </StackPanel>
</UserControl>
